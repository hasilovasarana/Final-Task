@model IEnumerable<crm_system.Models.Subscribe_Status>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sub_status_check)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.from_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.to_date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sub_status_check)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.from_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.to_date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sub_status_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.sub_status_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sub_status_id })
        </td>
    </tr>
}

</table>
